#include <stdio.h>
/**
 * main - main block
 * Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	{
		putchar(c);
	}
	
	putchar("\n");
	return (0);
}
